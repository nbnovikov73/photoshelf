# Codex Review Checklist

Before finishing a task, verify: project builds, no TypeScript errors, Astro remains the foundation, React is used only where needed, mobile admin flow still works, public pages remain fast, drafts are private, upload validation exists, image optimization is preserved, no secrets are committed, docs are updated if behavior changed.

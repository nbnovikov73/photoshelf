# Release Process

Before a release: build project, run lint, run tests if present, start with Docker Compose, verify login, upload, image processing, publish, public gallery, draft privacy, and README accuracy.

Do not release if the upload-to-publish flow is broken.

# Accessibility Guide

PhotoShelf must maintain baseline accessibility: semantic HTML, labels for inputs, visible focus states, keyboard navigation for admin controls, sufficient contrast, alt text support, and no image-only buttons without accessible labels.

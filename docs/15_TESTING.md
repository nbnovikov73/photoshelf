# Testing Strategy

Do not overbuild tests in MVP, but protect critical logic.

Priority areas: slug generation, upload validation, draft vs published visibility, image helper functions, API validation, auth protection.

Use small focused tests. Avoid brittle UI tests unless necessary.

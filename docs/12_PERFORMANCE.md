# Performance Budget

Public pages must be fast.

Targets: minimal JavaScript on public pages, optimized responsive images, lazy loading below the fold, no heavy UI libraries on public site, no unnecessary analytics in MVP.

Astro rule: if a component can be static Astro, it should not be a hydrated React component.
